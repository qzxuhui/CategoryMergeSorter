# compile
## linux
one can use the follow command to compile
```
g++ Main.cpp
```

and run the executable file
```
./a.out
```

## windows
directly use Microsoft Visual Studio or other integrate develop environment.

There is only a single file in whole project, it is really a simple case. If you want to modify the code, you must know the C++. I bet you have the enough knowledge how to compile it.